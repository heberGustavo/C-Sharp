@{
    Layout = "";
    @model IList<AdministrativoImperial.Domain.Models.EntityDomain.ObraDTO>
}

<table id="tabelaObra" class="table align-items-center mt-2 mb-0">
    <thead>
        <tr>
            <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Data</th>
            <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Funcionário</th>
            <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Obra</th>
            <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7 px-2">Ações</th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var item in Model)
        {
            <tr>
                <td>
                    <div class="d-flex px-3 py-1">
                        <div class="d-flex flex-column justify-content-center">
                            <h6 class="mb-0 text-sm">@item.da</h6>
                        </div>
                    </div>
                </td>
                <td class="align-middle">

                    <button class="btn btn-border-red" type="button" onclick="AlterarFuncao(@item.FnfId, '@item.FnfNome')" data-toggle="tooltip" data-original-title="Editar Função">
                        <span class="btn-inner--icon color-red"><i class="fas fa-edit"></i></span>
                    </button>
                    <button class="btn btn-border-blue btnExcluir" onclick="ConfirmarExclusao(@item.FnfId, '@item.FnfNome')" type="button" data-toggle="tooltip" data-original-title="Deletar Função">
                        <span class="btn-inner--icon color-blue"><i class="fa-solid fa-trash"></i></span>
                    </button>
                </td>
            </tr>
        }*@
    </tbody>
</table>